<h1>Editar nota</h1>

<form action="/editar-nota/<%= nota.id %>" method="POST" class="mt-4">
    <div class="form-group">
        <label for="titulo">Título:</label>
        <input type="text" class="form-control" name="titulo" value="<%= nota.titulo %>" required>
    </div>
    <div class="form-group">
        <label for="contenido">Contenido:</label>
        <textarea class="form-control" name="contenido" rows="5" required><%= nota.contenido %></textarea>
    </div>
    <button type="submit" class="btn btn-primary">Actualizar</button>
</form>

<a href="/notas" class="btn btn-danger mt-3">Volver</a>